# New Jerusalem Geometry v1.0.0 software-release boundary

## Status

`V1_0_0_SOFTWARE_RELEASE_BOUNDARY_READY_FOR_SIGNED_TAG`

Release date:

```text
2026-09-10
```

This checkpoint records the final software-release state prepared on `main`
after the verified private merge of the v1.0.0 release candidate.

It does not reopen the frozen scientific content.

## Parent merged-main boundary

The release metadata is applied after the signed private merge commit:

```text
47bb777  Merge v1.0.0 release candidate
```

The merged tree had already been independently verified on `main`.

## Active release state

```text
package version        1.0.0
CITATION.cff version   1.0.0
release date           2026-09-10
release branch         retained
repository visibility private through tag creation
Zenodo DOI             pending
```

## Verification state entering this boundary

The verified private merged-main state reported:

```text
804 passed
17 skipped
```

The 17 skips are historical package-version sentinels. They do not represent
failing applicable tests.

The manuscript built cleanly as an 18-page A4 PDF with no final LaTeX or
BibTeX warnings.

The frozen Phase 10A test surface retained SHA-256:

```text
2fa6c834d329de4c493a7566e620eabc3eaef338c2d18c6a1c14af8171535050
```

## Public-safety state

The pre-publication repository audit found no restricted Thompson Library
archival PDF, purchased Michell source PDF, private-source PDF, private key,
or credential-like secret in tracked Git history.

The only historical PDF objects are the six project-generated publication
figure PDFs.

## Scientific boundary

No geometry, theorem, numerical value, source calibration, construction
grammar, exact-sevenfold result, Plato extraction, Michell reconstruction, or
historical classification is changed by this final release-state commit.

## Tag boundary

After the final verification of this commit, a signed annotated tag:

```text
v1.0.0
```

is created pointing exactly to the final signed release commit.

The tag is verified locally before either `main` or the tag is pushed.

## Gates intentionally still downstream

```text
final public-safety verification after pushed tag
GitHub repository PRIVATE -> PUBLIC
enable Zenodo GitHub integration
create GitHub v1.0.0 Release
Zenodo archival deposit / DOI
propagate DOI into README / CITATION / manuscript / bibliography
journal-specific manuscript derivative / submission
release-branch deletion after tag reachability is reconfirmed
```
