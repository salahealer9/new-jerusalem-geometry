# v0.9.0 Exact Sevenfold Boundary and Minimal Cubic Extension

## Scope

Version `v0.9.0` seals the project's exact-sevenfold constructibility audit.

The release establishes:

```text
ordinary Euclidean construction:
    exact regular heptagon excluded

one cubic-capable angle trisection:
    exact regular heptagon confirmed
```

The minimum number of cubic-capable operations is therefore one within the
frozen operation-count model.

## Development boundary

The v0.9 branch began from:

```text
v0.8.0
5dfb0a2
```

The frozen v0.9 sequence is:

```text
d986a18  Preregister v0.9 exact-sevenfold constructibility audit
76397ad  Freeze v0.9 exact-sevenfold boundary audit
83dba52  Preregister v0.9 one-trisection exact-heptagon candidate
fb11105  Confirm v0.9 one-trisection exact-heptagon construction
c684f28  Freeze v0.9 exact-heptagon synthesis
```

## Euclidean constructibility result

For:

```text
y = 2*cos(2*pi/7)
```

the minimal polynomial is:

```text
y^3 + y^2 - 2*y - 1
```

It is irreducible over `Q`, so:

```text
degree(y) = 3
```

This is incompatible with the power-of-two degrees generated by a finite
straightedge-and-compass quadratic tower.

Frozen status:

```text
EUCLIDEAN_EXACT_SEVENFOLD_EXCLUDED
```

## Native-incidence census

The preregistered census checked:

```text
98 radial directions
4753 pairwise radial separations
22 native line directions
4873 semantic candidates
```

with seven target-injected comparator classes excluded.

Result:

```text
exact candidates = 0
NO_NATIVE_EXACT_SEVENFOLD
```

## One-trisection candidate

The Phase 9C candidate was frozen only after the Phase 9B boundary.

Its native anchor uses:

```text
R = 7
D = 3
u = 1
v = 3*sqrt(3)
H = 2*sqrt(7)
```

with exactly one:

```text
TRISECT_ANGLE
```

operation.

## Exact construction result

The trisection reconstruction gives exactly:

```text
z^3 - (7/3)z - 7/27 = 0
```

and after:

```text
y = z - 1/3
```

exactly:

```text
y^3 + y^2 - 2*y - 1 = 0
```

The interval `(1,2)` selects the same unique root as the frozen Phase 9B
sevenfold target.

All registered criteria pass:

```text
C1-C14 = 14 / 14
```

Terminal result:

```text
ONE_TRISECTION_EXACT_HEPTAGON_CONFIRMED
```

## Minimality

```text
Phase 9B:
0 cubic-capable operations are impossible

Phase 9D:
1 cubic-capable operation is sufficient
```

Therefore:

```text
MINIMUM_ONE_CUBIC_CAPABLE_OPERATION_WITHIN_FROZEN_MODEL
```

This is an operation-count statement, not a universal ranking of all
non-Euclidean construction technologies.

## v0.9 comparison synthesis

With fixed north orientation and no rotational fitting:

```text
Method-1 scaffold vs exact:
angular RMS   = 0.084260007737 deg
Euclidean RMS = 0.010294300316 u
              = 7.411896 model ft

Figure 14 plate vs exact:
angular RMS   = 0.224058788574 deg
radial RMS    = 0.016436995657 u
Euclidean RMS = 0.031953847393 u
              = 23.006770 model ft
```

The Figure 14 comparison is source-consistency synthesis and is not
statistically independent validation.

## Canonical visualization

```text
figures/generated/v0.9_exact_heptagon_synthesis.svg
```

Final Phase 9E SHA-256:

```text
4123b737b33d277531bf4b4a89ece680cbd0d0ab70393c0beb3abbc2f9df3eb9
```

Final encoding:

```text
charcoal solid = exact {7/2}
blue dashed    = Method-1 scaffold {7/2}
vermillion dot = affine Figure 14 plate
```

Two renderer-only legibility corrections were made after the first
deterministic synthesis render. Numerical JSON and Markdown remained
byte-identical throughout.

## Frozen v0.9 analytical artifacts

```text
data/analysis/njg_michell_v0_9/exact_sevenfold_boundary.json
cc150e2c92efdebe130f86f2c03998336883abb985513e8e634977682c84442f

data/analysis/njg_michell_v0_9/one_trisection_heptagon.json
f82d8fc9307ac12cb9541f24c090500d6ac7784bb11eb673eb89c9f2cb433dad

data/analysis/njg_michell_v0_9/exact_heptagon_synthesis.json
2d1b686f3fa9fdce3b5f218688bc15e7f538251a57865b5ff759f6f7dffdcfec
```

## Construction grammar

The frozen v0.8 central construction grammar is not modified by this release.

Version v0.9 records three PROJECT nodes in an additive extension registry:

```text
docs/specification/v0.9_construction_extension_nodes.csv
docs/specification/v0.9_construction_extension_dependencies.csv
docs/specification/v0.9_construction_extension_grammar.md

EXACT_SEVENFOLD_BOUNDARY
TRISECT_ANGLE_EXTENSION
EXACT_HEPTAGON_7
```

They are distinct from the historical/source-described Method 1 and Method 2
branches.

## Interpretation boundary

This checkpoint does not establish:

- Michell knowledge of the exact trisection construction;
- Sommerville knowledge of the exact trisection construction;
- intentional historical encoding of the exact heptagon;
- ancient provenance;
- physical or metaphysical significance;
- independent statistical validation from Figure 14;
- unique optimality of trisection among all cubic-capable technologies.

## Reproduction

```bash
python scripts/analyze_v0_9_exact_sevenfold_boundary.py
python scripts/analyze_v0_9_one_trisection_heptagon.py
python scripts/generate_v0_9_exact_heptagon_synthesis.py
pytest
```


## Release-test boundary

Fifteen frozen historical tests record the package-version boundary that applied
during v0.8/v0.9 development:

```text
15 historical package-version sentinels:
    each records a frozen 0.8.0 or pre-v0.9 release boundary
```

Those test files are retained byte-for-byte as provenance.

After the v0.9 closeout advances the active package metadata to `0.9.0`, the
six historical version assertions are no longer current-state invariants.

`tests/conftest.py` therefore marks exactly those fifteen node IDs as skipped only
when the active package version is `0.9.0`.

No analytical, geometric, protocol, source, or result test is skipped by this
release hook.

The default v0.9 release suite remains:

```bash
pytest
```

with the expected historical-boundary accounting:

```text
15 skipped historical version sentinels
all applicable tests pass
```
