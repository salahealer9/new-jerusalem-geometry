# New Jerusalem Geometry v1.0.0 release candidate

## Status

`V1_0_0_RELEASE_CANDIDATE_METADATA_APPLIED`

This checkpoint records the controlled transition of the active software and
citation metadata from `0.9.0` to `1.0.0`.

It does **not** reopen the frozen scientific content.

## Parent release-preparation boundary

The metadata transition was prepared from the signed branch checkpoint:

```text
3bbdd35  Record bounded Sommerville archival search outcome
```

on:

```text
plato-historical-context-v1.0
```

## Active metadata state

The following current-release surfaces now identify the package as `1.0.0`:

```text
CITATION.cff
pyproject.toml
src/new_jerusalem_geometry/__init__.py
tests/test_release_metadata.py
```

Historical v0.x references and frozen protocol statements are not
mass-rewritten.

The package development classifier advances from `Pre-Alpha` to
`Production/Stable` at this first stable release boundary.

## Citation-date boundary

The v0.9 release date is removed from `CITATION.cff`.

The actual `v1.0.0` `date-released` value is deliberately not guessed here.
It will be inserted on `main` in the final signed release commit immediately
before creation of the signed `v1.0.0` tag.

## Manuscript boundary

The manuscript now records the true release-candidate software version
`1.0.0`.

The public repository reference, Zenodo archival record, DOI, and final
software citation remain deferred until the public archival release exists.

No scientific result, geometry, numerical value, construction grammar,
source calibration, exact-sevenfold result, or historical interpretation is
changed by this checkpoint.

## Public-safety state

The pre-publication audit found no private archival scan, purchased Michell
source, credential, private key, or comparable restricted object in Git
history.

The Thompson Library research-use-only PDFs remain outside the repository.
Only provenance metadata, filenames, page counts, and cryptographic hashes are
tracked.

## Gates intentionally still open

```text
merge release candidate into main while repository remains private
verify main
insert actual v1.0.0 release date
create final signed release commit
create signed v1.0.0 tag
final public-safety gate
change GitHub repository PRIVATE -> PUBLIC
enable Zenodo integration
create GitHub v1.0.0 Release
obtain Zenodo DOI
propagate DOI to README / CITATION / manuscript / bibliography
submit manuscript
```

## Branch lifecycle

The release branch remains in place until the signed `v1.0.0` tag is verified
as reachable from `main`.
