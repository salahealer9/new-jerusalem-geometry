# v0.7 Phase 7C — dodecagon geometry-freeze and comparison-rendering protocol

## Status

Preregistered after the frozen Phase 7B vertex-radius audit and before
generation of the canonical reconstruction/comparison SVGs.

## Phase 7B result inherited unchanged

Phase 7B confirmed:

```text
TWO_SEMANTIC_RADIUS_CLASSES_CONFIRMED
```

from the already-frozen v0.5 `POLAR_PIVOT_WALL`.

The semantic classes are:

```text
POLAR_ADJACENT_VERTEX
    count = 8

OBLIQUE_PAIR_VERTEX
    count = 4
```

The regular dodecagon baseline uses the inherited apothem `8.5 u` and introduces
no fitted parameter.

Phase 7B found:

```text
regular baseline radius       8.79984753348571 u
polar-adjacent mean radius    8.79984753348571 u
oblique-pair mean radius      8.75294593972281 u
oblique inward displacement  -0.0469015937628932 u
```

The exact frozen Phase 7B JSON artifact is:

```text
data/analysis/njg_michell_v0_7/dodecagon_vertex_radius_audit.json
```

with SHA-256:

```text
7093250082940c16b1d1df240e791ba5d5e31b7aa10a1866d58ec8e00d3c96b5
```

## Phase 7C scientific decision

No separate dodecagon reconstruction algorithm is required.

The relevant source geometry already exists in the frozen v0.5 wall.

Accordingly, Phase 7C records the preferred reconstruction as:

```text
SOMMERVILLE_DODECAGON_RECONSTRUCTION
    = frozen v0.5 POLAR_PIVOT_WALL
      interpreted through the Phase 7A source clarification
      and Phase 7B semantic vertex-radius audit
```

This is an interpretation/freeze operation, not a new geometric fit.

## No-new-geometry rule

Phase 7C introduces:

```text
new continuous geometric parameters = 0
new fitted scale parameters          = 0
new discrete candidate choices       = 0
new wall vertices                    = 0
new wall sides                       = 0
```

The Phase 7C renderer must consume the frozen Phase 7B JSON artifact.

It must not recompute or modify the preferred wall geometry independently.

## Historical-target firewall

The following historical dimensional values remain prohibited from Phase 7C
geometry/rendering logic:

```text
6336
6300
176:175
31680 / 5
```

The Phase 7C SVGs must contain normalized geometry only.

No current-foot scale or historical residual is allowed.

## Canonical reconstruction SVG

Output:

```text
figures/generated/sommerville_dodecagon_reconstruction.svg
```

Purpose:

Show the preferred source-faithful dodecagon geometry as the already-frozen
polar-pivot wall.

It must visually distinguish:

- the 8 `POLAR_ADJACENT_VERTEX` vertices;
- the 4 `OBLIQUE_PAIR_VERTEX` vertices;
- polar wall sides;
- oblique wall sides;
- the common construction centre;
- normalized radius rays for the two semantic classes.

The SVG must not display historical dimensional target values.

## Canonical comparison SVG

Output:

```text
figures/generated/sommerville_dodecagon_comparison.svg
```

Purpose:

Compare the parameter-free regular-dodecagon baseline with the frozen
polar-pivot reconstruction.

It may display:

- regular baseline polygon;
- frozen polar-pivot polygon;
- vertex correspondences;
- inward displacement at the four oblique-pair vertices;
- normalized radius-class labels;
- normalized displacement values;
- the raw normalized class ratio.

It must not display or compare with historical dimensional targets.

## Provenance

Both SVGs must record, directly or through deterministic embedded metadata:

- Phase 7C protocol path/hash;
- Phase 7B JSON path/hash;
- v0.5 geometry artifact path/hash;
- reconstruction identifier;
- statement that historical targets were not loaded.

No absolute machine paths are allowed.

## Deterministic generation

Both SVGs must regenerate byte-identically.

## Interpretation boundary

Phase 7C establishes only that the frozen v0.5 wall has the geometry required
to represent the Figure 30 two-radius dodecagon structure.

It does not yet claim numerical agreement with the historical dimensional
values.

That comparison belongs downstream after this geometry/rendering boundary is
frozen.

## Phase 7C stopping status

Successful completion records:

```text
EXISTING_FROZEN_WALL_IS_COMPLETE_DODECAGON_GEOMETRY
```

Any need to modify wall geometry requires stopping and documenting the reason
before proceeding.
