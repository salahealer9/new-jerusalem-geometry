# v0.9 exact-sevenfold project-extension grammar

## Status

This file is an additive v0.9 project-extension registry.

It deliberately does **not** modify the frozen central construction grammar
sealed at v0.8.0.

The historical/source-described Method 1 and Method 2 branches remain exactly
as they were at the v0.8 boundary.

## Project extension

```text
CONSTRUCTION_CIRCLE
        |
        v
EXACT_SEVENFOLD_BOUNDARY
        |
        | Euclidean exactness excluded
        v
TRISECT_ANGLE_EXTENSION
        |
        | exactly one cubic-capable operation
        v
EXACT_HEPTAGON_7
```

`EXACT_SEVENFOLD_BOUNDARY` records the Phase 9B theorem-level result:

```text
target polynomial:
y^3 + y^2 - 2*y - 1

degree over Q:
3

ordinary Euclidean closure:
excluded
```

The finite native-incidence census is supporting project analysis and does not
replace the constructibility theorem.

`TRISECT_ANGLE_EXTENSION` is explicitly a PROJECT construction extension, not
a Michell or Sommerville source node.

Its frozen native anchor is:

```text
R = 7
D = 3
u = R - 2D = 1
v = D*sqrt(3) = 3*sqrt(3)
H = 2*sqrt(7)
```

The extension admits exactly one cubic-capable operation:

```text
TRISECT_ANGLE
```

and yields the project node:

```text
EXACT_HEPTAGON_7
```

on the radius-7 construction circle.

Operational minimality:

```text
zero cubic-capable operations:
    impossible

one cubic-capable operation:
    sufficient
```

within the frozen v0.9 operation-count model.

No node or dependency in this additive registry attributes the exact-heptagon
extension to Michell, Sommerville, or an ancient source.

The registry does not claim that angle trisection is uniquely simplest among
all possible non-Euclidean construction technologies.

The Phase 9E comparisons and SVG are downstream synthesis only. They do not
feed back into the Phase 9B theorem or Phase 9D exact construction proof.
