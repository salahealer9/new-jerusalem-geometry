# v1.0 release-readiness result

## Purpose

This document records the release-readiness state after completion of the v1.0
scientific work and public-repository hygiene audit.

It does not itself perform or claim the v1.0.0 software release.

## Scientific-content gate

Status:

```text
PASS
```

The scientific content is frozen.

No further geometry, historical-source interpretation, numerical fitting, or
new research hypothesis is permitted inside the v1.0.0 release boundary.

## Deterministic reconstruction gate

Status:

```text
PASS
```

The Plato–Michell numerical reconstruction remains a zero-search deterministic
derivation:

```text
free parameters          0
nearest-match choices    0
optimisations            0
permutation searches     0
scale fits               0
```

## Visual-capstone gate

Status:

```text
PASS
```

Canonical figure:

```text
figures/generated/v1.0_plato_michell_whorl_capstone.svg
```

Frozen SHA-256:

```text
379bcbecf524890a055b3aa8cc926f54320cd8fffd8adacf29f63fdc68bf81d9
```

The figure is deterministic and downstream of the frozen Phase 10E JSON.

## Historical-boundary gate

Status:

```text
PASS
```

The project preserves the distinction:

```text
DIRECT_PLATO_TEXT
MICHELL_EXPLICIT
PROJECT_RECONSTRUCTION
```

The release does not claim that Plato supplied Michell's numerical widths or
intended Michell's numerical assignment.

## Third-party licensing gate

Status:

```text
PASS
```

The two frozen Perseus Plato XML witnesses retain their exact project hashes and
their third-party CC BY-SA 4.0 licensing boundary is explicitly documented.

No tracked private source-working directory is included in the repository.

## Public-repository hygiene gate

Status:

```text
PASS
```

The closeout audit found no tracked credentials, private keys, secret files,
private source scans, archives, or accidental machine-specific working paths
requiring removal before public release.

## Automated-test gate

Status:

```text
PASS
```

Complete repository suite:

```text
806 passed
15 skipped
```

Applicable tests pass.

## Working-tree gate

Required status before sealing this result:

```text
clean
```

## Version gate

Current package version at this release-readiness checkpoint:

```text
0.9.0
```

This is intentional.

The final change to:

```text
1.0.0
```

belongs to the release metadata commit after the paper and public-facing release
surfaces are ready.

## Current-release metadata gate

Status:

```text
PENDING_RELEASE_ENGINEERING
```

The following current-release surfaces still require the controlled final
v1.0.0 update:

```text
README.md
CITATION.cff
pyproject.toml
src/new_jerusalem_geometry/__init__.py
```

Historical v0.x references are preserved and must not be mass-replaced.

## Paper gate

Status:

```text
PENDING
```

No manuscript is currently tracked in the software repository.

The paper will be prepared from the frozen v1.0.0 scientific content without
reopening the analysis.

## Branch / merge gate

Current branch:

```text
plato-historical-context-v1.0
```

Status:

```text
PENDING_MERGE_TO_MAIN
```

The branch should remain in place while release preparation is completed.

The intended sequence is:

```text
complete release candidate on plato-historical-context-v1.0
        |
        v
verify clean full-suite state
        |
        v
merge into main while repository remains private
        |
        v
verify main
        |
        v
apply/seal final v1.0.0 release boundary as appropriate
        |
        v
signed v1.0.0 tag
        |
        v
public GitHub release / Zenodo archive
        |
        v
delete obsolete release branch
```

The branch must not be deleted until the merge and release reference have been
verified.

## Publication gates still pending

```text
paper finalisation
final current-release README
final CITATION.cff metadata
0.9.0 -> 1.0.0 version update
main-branch merge
signed v1.0.0 tag
repository visibility change
GitHub release
Zenodo archival DOI
final DOI/citation propagation
```

## Readiness conclusion

The scientific and public-hygiene layers are ready for release engineering.

Status:

```text
V1_0_SCIENTIFIC_CONTENT_FROZEN
PUBLIC_REPOSITORY_HYGIENE_PASS
READY_FOR_RELEASE_PREPARATION
```

This is not yet the software release itself.
