# v0.5 release-readiness result

## Status

```text
READY_FOR_V0_5_0
```

The final Phase 5G.2 audit was run after the provenance-path portability
remediation committed as:

```text
567ba23 Normalize Phase 5F provenance paths
```

The audit was performed while the package version remained `0.4.0`.
No release-version metadata was changed before the readiness verdict.

## Test suite

The complete repository test suite passed both before and after deterministic
canonical-artifact regeneration:

```text
322 passed
```

## Integrated generative object audit

The canonical geometry export contains:

```text
132 objects
```

The canonical provenance manifest contains:

```text
132 objects
```

Their complete ordered `(object_id, geometry_type)` identities agree.

The rendered provenance-linked subset contains:

```text
59 objects
```

The canonical SVG contains the same 59 provenance-linked object IDs.

Rendered semantic counts remain:

```text
12 Moon circles
28 scaffold markers
7 heptagram vertices
7 heptagram edges
```

The scaffold roles remain:

```text
12 moon centres
8 inter-Moon gaps
8 intersection positioners
```

## Evidence and generative boundaries

The geometry export contains no provenance/evidence classification fields.

The audited generative modules do not import Figure 12 or Figure 14
calibration, digitisation, registration, source-coordinate, source-fitting,
or forward-validation modules.

Source-calibration analyses therefore remain downstream of the generative
construction path.

## Phase 5E

The registered generative invariance audit remains `PASS`.

Registered scales:

```text
0.25
0.73
1.00
2.75
8.00
```

## Phase 5F Stage A

The grammar-only result remains:

```text
GRAMMAR_UNDERDETERMINED
```

All four registered sevenfold residue classes survive:

```text
0
1
2
3
```

The historical Stage A artifact remains preserved at commit:

```text
a84949a
```

with SHA-256:

```text
48a96dc7e160365cafc9114d7586ce94b070ebbe0403d759e71e8795c28d6d7d
```

Phase 5G normalized only the machine-specific
`protocol.protocol_path` serialization.

The portable Stage A representation has SHA-256:

```text
6bfb543d5fbb98cbd383a242eac109d1ff0f7a96a68410d370ed294ec119613b
```

The portability comparison established that no Stage A scientific value
changed.

## Phase 5F Stage B

Stage B remains:

```text
COMPLETE
```

The frozen no-refit descriptive Figure 14 source-consistency ranking is:

```text
3
0
2
1
```

The preferred source-consistent candidate is:

```text
phase 3
```

This does not alter the Stage A grammar result.

The final interpretation flags remain:

```text
independent_validation = false
feeds_back_into_builder = false
grammar_selection_changed = false
```

Accordingly, the scientific conclusion remains:

> The construction grammar alone leaves the four C4-related sevenfold phases
> underdetermined. Among those four frozen candidates, the printed Figure 14
> source plate strongly favors phase 3 under the registered no-refit
> source-consistency comparison.

This is not an independent prediction and does not establish grammar-derived
phase uniqueness.

## Canonical artifact hashes

The final portable canonical hashes entering the v0.5.0 checkpoint are:

```text
SVG
7d8d2e7cd8a07e56791b850119febd5565f0dbedf6059e835589380e4ebb6d67

geometry JSON
19e22356378d581a2adb35c8f8b8ee0237f61ab365d8cbc05abc378568e3b6cb

provenance JSON
62d3bac01dc6df00ac79a38bb9a7e7d7f3a466552b8304cfc12a79075af050e5

Phase 5F analysis JSON
e694593cb5459ca45453167c2e65be0b37539e824bd341384edb25df5c5cc637
```

All four regenerated byte-identically during the final readiness audit.

## Portability

The canonical geometry, provenance, and Phase 5F JSON artifacts contain no
machine-specific `/home/...` or `\Users\...` paths.

The Phase 5F artifact records both the historical Stage A freeze and the
portable canonical Stage A representation.

## Release-readiness verdict

The final integrated audit returned:

```text
FINAL INTEGRATED STRUCTURAL AUDIT: PASS
READY_FOR_V0_5_0
```

The next operation is the controlled metadata transition from `0.4.0` to
`0.5.0`. No geometric model, source calibration, Phase 5E result, or Phase 5F
scientific result is changed by that metadata transition.
