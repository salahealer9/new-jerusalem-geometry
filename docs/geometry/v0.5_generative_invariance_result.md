# v0.5 NJG_MICHELL generative closure and invariance result

## Status

Phase 5E evaluates the preregistered generative-closure and scale-invariance
protocol for the integrated `NJG_MICHELL` construction.

The registered outcome is:

```text
PASS
````

All registered scale laws, discrete invariants, cross-representation identity
checks, and same-input determinism checks passed.

## Preregistered scale set

The audit used exactly the five scales frozen before evaluation:

```text
0.25
0.73
1.00
2.75
8.00
```

The largest construction is therefore 32 times the linear scale of the
smallest.

No registered scale was removed or replaced after evaluation.

## Test result

At completion of Phase 5E the full repository suite reports:

```text
280 passed
```

The project package version remains:

```text
0.4.0
```

during v0.5 development.

## Generative path tested

For every registered scale the complete downstream path was exercised:

```text
unit
  |
  v
build_michell_composite
  |
  +-- numerical geometry export
  +-- provenance export
  +-- SVG renderer
```

No source-plate calibration geometry participates in this scale audit.

## Numerical scale audit

Representative numerical outputs are:

```text
scale  wall perimeter       wall area            mean wall side       sevenfold step deg     scaffold beta deg      closure mismatch rad       first star edge
0.25   13.641582524345997   14.467906005587594   1.1367985436954997   51.470701432439967     25.587895702680132     2.2204460492503131e-16    2.738014046805906
0.73   39.833420971090312   123.359153766042     3.3194517475908594   51.470701432439967     25.587895702680132     2.2204460492503131e-16    7.9950010166732444
1.00   54.566330097383990   231.4864960894015    4.5471941747819988   51.470701432439967     25.587895702680132     2.2204460492503131e-16    10.952056187223624
2.75   150.05740776780598   1750.616626676099    12.504783980650499   51.470701432439967     25.587895702680132     2.2204460492503131e-16    30.118154514864973
8.00   436.53064077907192   14815.135749721696   36.377553398255991   51.470701432439967     25.587895702680132     2.2204460492503131e-16    87.616449497788992
```

The observed behavior agrees with the preregistered dimensional laws.

## Linear scale laws

The audit confirms:

```text
Q(s) = s Q(1)
```

within the registered floating-point tolerances for all tested linear
quantities.

These include:

```text
core coordinates and dimensions
Moon centres and radii
wall offsets
wall tangency points
wall vertices
wall side lengths
wall perimeter
wall mean side length
sevenfold radius and points
fourteenfold points
scaffold radius and points
heptagram vertices
heptagram edge lengths
```

No downstream geometric family showed evidence of a hidden unit-one length.

## Quadratic scale law

The polar-pivot wall area satisfies:

```text
A(s) = s^2 A(1)
```

at every registered scale within the preregistered numerical tolerance.

For example:

```text
A(1) = 231.4864960894015
A(8) = 14815.135749721696
```

and:

```text
A(8) / A(1) = 64
```

within floating-point precision.

Likewise the quarter-scale area is one sixteenth of the unit-one area.

## Angular invariance

The following quantities remain invariant across all five scales:

```text
wall-line normals
sevenfold step
sevenfold angular positions
fourteenfold angular positions
fourteenfold angular gaps
four-triangle angular marks
scaffold beta
scaffold heptagonal step
scaffold point angles
```

The representative sevenfold step remains:

```text
51.470701432439967 degrees
```

and the scaffold beta remains:

```text
25.587895702680132 degrees
```

throughout the scale audit.

## Four-triangle/scaffold closure

The supplied composite reports:

```text
four_triangle_scaffold_max_mismatch_radians
    =
2.2204460492503131e-16
```

for every registered scale.

This residual is angular and therefore correctly remains scale invariant.

The result is consistent with numerical floating-point closure of the two
independently represented 28-mark constructions.

## Moon-group invariance

The complete four-by-three Moon grouping remains exactly identical at every
registered scale:

```text
east:
    moon-q4-b
    moon-east
    moon-q1-a

north:
    moon-q1-b
    moon-north
    moon-q2-a

west:
    moon-q2-b
    moon-west
    moon-q3-a

south:
    moon-q3-b
    moon-south
    moon-q4-a
```

No tolerance is involved in this result.

The names, group directions, ordering, cardinal members, and outer members
compare exactly.

## Wall semantic invariance

The twelve wall-line/Moon correspondences remain exactly:

```text
wall_east        -> moon-east
wall_east_north  -> moon-q1-a
wall_north_east  -> moon-q1-b
wall_north       -> moon-north
wall_north_west  -> moon-q2-a
wall_west_north  -> moon-q2-b
wall_west        -> moon-west
wall_west_south  -> moon-q3-a
wall_south_west  -> moon-q3-b
wall_south       -> moon-south
wall_south_east  -> moon-q4-a
wall_east_south  -> moon-q4-b
```

at all five scales.

This confirms that scale changes do not alter the wall construction topology or
its semantic association with the twelve-Moon system.

## Scaffold-role invariance

The complete ordered 28-point role sequence is unchanged across every scale.

It continues to contain:

```text
12 moon_centre positions
8 inter_moon_gap positions
8 intersection_positioner positions
```

with identical indices, quadrants, local indices, and role labels.

## Figure 14 candidate invariance

The currently implemented scaffold-derived Figure 14 candidate remains tied to
exactly the same scaffold indices at every scale:

```text
7, 11, 15, 19, 23, 27, 3
```

The heptagram traversal remains:

```text
0, 2, 4, 6, 1, 3, 5
```

and the edge pairs remain:

```text
(0, 2)
(2, 4)
(4, 6)
(6, 1)
(1, 3)
(3, 5)
(5, 0)
```

The corresponding edge lengths scale linearly.

Phase 5E establishes only scale invariance of this already selected candidate.

It does not establish historical preference for this scaffold phase.

That question remains reserved for Phase 5F.

## Object identity

At every registered scale:

```text
geometry object count   = 132
provenance object count = 132
```

The complete ordered:

```text
(object_id, geometry_type)
```

sequence remains identical.

Therefore scale has no effect on the machine-readable object namespace.

## Provenance invariance

After removing only the explicit top-level `unit` field, the provenance
representation remains invariant across all registered scales.

This includes:

```text
source hashes
object records
grammar nodes
construction dependencies
claims
grammar-node memberships
source-object relationships
rendered-SVG identities
```

Consequently geometric scale does not modify the evidential classification of
the construction.

In particular, scale does not alter:

```text
node_type
relation_type
evidence_status
source_status
claim_ids
```

## SVG structural invariance

Every registered scale preserves:

```text
12 Moon-circle elements
28 scaffold markers
7 heptagram vertices
7 heptagram edges
59 provenance-linked SVG objects
```

The set of SVG `data-provenance-id` values is exactly invariant.

The SVG byte streams are not required to match across different scales because
their numerical geometry differs.

## Three-way representation identity

At every registered scale the three-way relationship remains valid:

```text
geometry JSON
      |
      | object_id
      v
provenance JSON
      |
      | rendered object_id
      v
SVG
```

All 132 geometry objects resolve to the same provenance namespace.

The 59 rendered provenance objects correspond exactly to the 59 SVG
`data-provenance-id` values.

## Same-input determinism

For every registered scale, two independently constructed composites with the
same input produce byte-identical:

```text
geometry JSON
provenance JSON
SVG
```

This demonstrates deterministic downstream generation independently of the
cross-scale comparisons.

## Evidence separation

The geometry representation remains free of:

```text
evidence_status
source_status
relation_type
node_type
claim_ids
```

throughout the multi-scale audit.

The geometry and provenance layers therefore remain separate even while their
object identities are cross-validated.

## Generative closure result

The complete implemented construction succeeds over the registered scale range
without scale-specific correction.

The Phase 5E result is therefore:

```text
PASS
```

for the preregistered criteria:

```text
linear scaling          PASS
quadratic scaling       PASS
angular invariance      PASS
Moon grouping           PASS
wall topology           PASS
scaffold roles          PASS
Figure 14 candidate     PASS
object identity         PASS
provenance invariance   PASS
SVG structure           PASS
three-way joins         PASS
same-input determinism  PASS
```

## Interpretation

Phase 5E demonstrates that the implemented `NJG_MICHELL` composite behaves as
an internally generative, scale-covariant Euclidean construction across the
tested scale interval.

The result specifically rules out, within the tested paths, accidental
dependence on the canonical `unit = 1` normalization.

It also confirms that scale changes do not alter the construction's discrete
topology, semantic roles, provenance classifications, or representation
identity.

This result does not establish:

```text
historical uniqueness
historical intent beyond documented evidence
independent source validation
statistical significance
physical significance
historical preference of the current Figure 14 scaffold phase
```

Those remain separate questions.

## Phase 5F boundary

Phase 5F will audit the remaining discrete sevenfold/scaffold phase ambiguity.

The four candidate residue classes remain:

```text
phase 0: 0, 4, 8, 12, 16, 20, 24
phase 1: 1, 5, 9, 13, 17, 21, 25
phase 2: 2, 6, 10, 14, 18, 22, 26
phase 3: 3, 7, 11, 15, 19, 23, 27
```

Nothing in the Phase 5E scale-invariance result promotes phase 3 over the other
three.

The project version remains `0.4.0` until the final v0.5 integration
checkpoint.
