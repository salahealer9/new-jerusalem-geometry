# v0.9 Exact Sevenfold Boundary and Minimal Cubic Extension

## Scope

Version `v0.9.0` asks a sharply defined mathematical question arising from the
New Jerusalem geometry reconstructed through v0.8:

> What is the exact straightedge-and-compass constructibility boundary of the
> sevenfold layer, and what is the minimum number of cubic-capable operations
> required to cross that boundary within the frozen project model?

The result has two complementary parts:

```text
ordinary Euclidean closure:
    exact regular sevenfold excluded

one registered cubic-capable operation:
    exact regular heptagon confirmed
```

The release does not attribute the exact construction to John Michell, Ian
Sommerville, or any historical source.

## Frozen development sequence

```text
d986a18  Preregister v0.9 exact-sevenfold constructibility audit
76397ad  Freeze v0.9 exact-sevenfold boundary audit
83dba52  Preregister v0.9 one-trisection exact-heptagon candidate
fb11105  Confirm v0.9 one-trisection exact-heptagon construction
c684f28  Freeze v0.9 exact-heptagon synthesis
```

Parent release:

```text
v0.8.0
5dfb0a2
```

## Euclidean constructibility boundary

Let:

```text
y = 2*cos(2*pi/7)
```

The target satisfies the irreducible cubic:

```text
y^3 + y^2 - 2*y - 1 = 0
```

so the target has algebraic degree `3` over `Q`.

Straightedge-and-compass coordinates lie in finite towers of quadratic
extensions, so their algebraic degrees are powers of two.

Therefore:

```text
EUCLIDEAN_EXACT_SEVENFOLD_EXCLUDED
```

This is a theorem-level constructibility result, not merely a failed finite
search.

## Native-incidence census

The preregistered finite census checked:

```text
N0 lineage nodes                 = 14
N1 lineage checks                = 2
semantic radial directions      = 98
pairwise radial separations     = 4753
native straight-line directions = 22
semantic candidates             = 4873
target-injected classes excluded= 7
```

Exact classification gave:

```text
exact native candidates = 0
NO_NATIVE_EXACT_SEVENFOLD
```

The nearest numerical candidate was retained for descriptive geometry only:

```text
candidate = separation/021-078
angle     = 51.45244386112276 deg
residual  = 0.02387243255133123 deg
```

It does not weaken or replace the constructibility theorem.

## Target-injection boundary

The audit explicitly excluded exact-sevenfold objects that already contain the
answer by definition, including:

```text
ObliqueModel.DIVISION_28
NJG_28 generated from DIVISION_28
build_regular_heptagram
free regular-seven fits
direct 2*pi/7 reference objects
```

They remain valid downstream comparators but cannot count as native
discoveries.

## One-trisection project extension

After the Euclidean boundary was frozen, one specific cubic-capable candidate
was preregistered.

Using the native dimensions:

```text
R = 7
D = 3

u = R - 2D = 1
v = D*sqrt(3) = 3*sqrt(3)
H = sqrt(u^2 + v^2) = 2*sqrt(7)
```

the native right-triangle anchor gives:

```text
tan(Theta) = 3*sqrt(3)
cos(Theta) = 1/(2*sqrt(7))
```

The sole cubic-capable operation is:

```text
TRISECT_ANGLE

phi = Theta/3
```

## Exact cubic reconstruction

Set:

```text
z = (2*sqrt(7)/3)*cos(phi)
```

The triple-angle identity gives exactly:

```text
z^3 - (7/3)z - 7/27 = 0
```

and with:

```text
y = z - 1/3
```

one obtains exactly:

```text
y^3 + y^2 - 2*y - 1 = 0
```

The constructed root is selected by an exact interval argument, not by
numerical proximity.

The constructed `y` lies in `(1,2)`, and the cubic is strictly increasing on
that interval. The Phase 9B sevenfold target is the unique root there.

Therefore:

```text
y = 2*cos(2*pi/7)
```

exactly.

## Exact regular heptagon

The Phase 9D audit passed all fourteen registered criteria:

```text
C1-C14 = 14 / 14
```

with terminal status:

```text
ONE_TRISECTION_EXACT_HEPTAGON_CONFIRMED
```

Floating diagnostics were at machine precision:

```text
constructed y               = 1.2469796037174672
constructed c = y/2         = 0.6234898018587336
numeric y-target residual   = 0.0
numeric angle residual      = 0.0
max radius residual         = 1.7763568394002505e-15
side-length range           = 2.6645352591003757e-15
seven-step closure residual = 3.580361673049448e-15
```

The exactness proof is algebraic; these residuals are implementation
diagnostics.

## Minimality within the frozen model

Together:

```text
Phase 9B:
0 cubic-capable operations are impossible

Phase 9D:
1 cubic-capable operation is sufficient
```

Therefore:

```text
MINIMUM_ONE_CUBIC_CAPABLE_OPERATION_WITHIN_FROZEN_MODEL
```

This is a minimum in the NUMBER of cubic-capable operations relative to the
frozen Euclidean grammar. It is not a claim that angle trisection is uniquely
simplest among all possible non-Euclidean construction technologies.

## v0.8 approximations versus the exact v0.9 result

| Construction | Step (deg) | Signed error (deg) | Seven-step closure (deg) |
| --- | ---: | ---: | ---: |
| Exact one-trisection | 51.428571428571 | +0.000000000000 | +0.000000000000 |
| Michell Method 1 | 51.470701432440 | +0.042130003869 | +0.294910027080 |
| Michell Method 2 | 51.317812546511 | -0.110758882061 | -0.775312174426 |

Thus:

```text
Method 2 < exact < Method 1
```

## Exact versus frozen Method-1 scaffold

With fixed north orientation and no rotational fitting:

```text
angular RMS       = 0.084260007737 deg
angular maximum   = 0.126390011606 deg
Euclidean RMS     = 0.010294300316 u
Euclidean maximum = 0.015441449778 u
Euclidean RMS     = 7.411896 model ft
```

This is a source-consistency comparison of a frozen project candidate.

## Exact versus Figure 14 plate endpoints

The affine-calibrated Figure 14 source endpoints are compared in their existing
semantic order with no rotational fit and no endpoint reassignment:

```text
angular RMS       = 0.224058788574 deg
angular maximum   = 0.470051178465 deg
radial RMS        = 0.016436995657 u
radial maximum    = 0.034703110919 u
Euclidean RMS     = 0.031953847393 u
Euclidean maximum = 0.059258802952 u
Euclidean RMS     = 23.006770 model ft
```

This is not statistically independent validation because Figure 14 source
calibration and topology informed earlier project development.

## Twelvefold architectural context

The exact heptagon lies on:

```text
R = 7 u
```

The preferred dodecagon belongs to a different radial layer:

```text
wall vertex radius range
8.752945939723 .. 8.799847533486 u
```

No nearest-wall-vertex sevenfold comparison was searched.

## Visual capstone

The deterministic transparent synthesis SVG is:

```text
figures/generated/v0.9_exact_heptagon_synthesis.svg
```

Final Phase 9E SHA-256:

```text
4123b737b33d277531bf4b4a89ece680cbd0d0ab70393c0beb3abbc2f9df3eb9
```

It uses redundant encoding:

```text
dark charcoal solid = exact {7/2}
blue dashed         = Method-1 scaffold {7/2}
vermillion dotted   = affine Figure 14 plate
```

The first deterministic Phase 9E render had two presentation-only legibility
issues. Post-result renderer-only corrections changed subtitle placement,
stroke hierarchy, draw order, and color encoding. The numerical JSON,
Markdown, orientation, endpoint correspondence, and geometric comparisons
remained byte-identical.

## Construction-grammar interpretation

The central construction grammar remains frozen at its v0.8 boundary.

Version v0.9 records its PROJECT extension in additive files:

```text
docs/specification/v0.9_construction_extension_nodes.csv
docs/specification/v0.9_construction_extension_dependencies.csv
docs/specification/v0.9_construction_extension_grammar.md
```

The additive project layer is:

```text
CONSTRUCTION_CIRCLE
        |
        v
EXACT_SEVENFOLD_BOUNDARY
        |
        v
TRISECT_ANGLE_EXTENSION
        |
        v
EXACT_HEPTAGON_7
```

The native dimensions `7` and `3` parameterize the trisection anchor.

This extension is intentionally distinct from, and does not mutate, Michell's
frozen source-described Method 1 and Method 2 grammar branches.

## Interpretation boundary

Version v0.9 establishes:

1. the exact regular sevenfold target is excluded from the frozen ordinary
   straightedge-and-compass closure;
2. the frozen native-incidence census contains no exact target after
   target-injected references are excluded;
3. one preregistered exact angle trisection, anchored by the native `7` and `3`
   dimensions plus equilateral-triangle geometry, is sufficient to construct
   the exact regular heptagon;
4. zero cubic-capable operations are impossible and one is sufficient within
   the frozen operation-count model.

It does NOT establish:

- that Michell knew the one-trisection construction;
- that Sommerville knew the one-trisection construction;
- intentional historical encoding of the exact heptagon;
- ancient provenance;
- physical, archaeological, astronomical, or metaphysical significance;
- statistical independence of the Figure 14 comparison;
- that angle trisection is uniquely simplest among all non-Euclidean
  construction technologies.

## Reproduction

```bash
python scripts/analyze_v0_9_exact_sevenfold_boundary.py
python scripts/analyze_v0_9_one_trisection_heptagon.py
python scripts/generate_v0_9_exact_heptagon_synthesis.py
pytest
```
